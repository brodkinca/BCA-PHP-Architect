/**
 * PDEPEND: PHP Depend
 */

# Path to source code.
pdepend.src=${dir.src}

/**
 * PHPCPD: PHP Copy/Paste Detector
 */

# Path to source code.
phpcpd.src=${dir.src}

/**
 * PHPCS: PHP_CodeSniffer
 */

# Name or path of the coding standard to use.
phpcs.standard=${dir.vendor}/bca/phpcs-standard/phpcs.xml
phpcs.fileset=${dir.src} ${dir.tests}

/**
 * PHPDOC: phpDocumentor
 */

# Path to source code.
phpdoc.src=${dir.src}

/**
 * PHPMD: PHP Mess Detector
 */

# Comma-separated PHPMD rules.
phpmd.ruleset=codesize,design,naming,unusedcode
# Comma-separated path(s) to analyze.
phpmd.src=${dir.src}

